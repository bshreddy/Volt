# Host Function Support Extension in CuPBoP

CuPBoP is designed so that CUDA-facing behavior can be extended, rather than by modifying the core compiler pipeline. This modular design makes it straightforward to introduce or refine host-runtime/memory behaviors needed by different open GPU architectures, while keeping the kernel translation infrastructure reusable. 
